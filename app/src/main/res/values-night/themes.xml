<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Premium Splash Screen Theme (Dark Mode) -->
    <style name="Theme.RemindMe.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.RemindMe</item>
    </style>
</resources>
